<% if application.team && application.dependencies_team &&
      application.team != application.dependencies_team %>
  <strong><%= application.team %></strong> owns the app. <strong><%= application.dependencies_team %></strong> is responsible for updating its dependencies.
<% elsif application.team %>
  <%= application.team %>
<% elsif application.dependencies_team %>
  <%= application.dependencies_team %>
<% else %>
  N/A
<% end %>
