---
layout: content_schema
title:  <%= display_name %>
---

## Publisher content schema

This is what publisher apps send to the publishing-api.

[Schema on GitHub](https://raw.githubusercontent.com/alphagov/govuk-content-schemas/master/dist/formats/<%= schema_name %>/publisher_v2/schema.json)

<%= table_of_properties(publisher_properties) %>

<% if renderable? %>

---

## Publisher links schema

The links for this item.

[Schema on GitHub](https://raw.githubusercontent.com/alphagov/govuk-content-schemas/master/dist/formats/<%= schema_name %>/publisher_v2/links.json)

<%= table_of_properties(links_properties) %>

---

## Frontend schema

This is the schema for what you'll get back from the content-store.

[Schema on GitHub](https://raw.githubusercontent.com/alphagov/govuk-content-schemas/master/dist/formats/<%= schema_name %>/frontend/schema.json)

<%= table_of_properties(frontend_properties) %>

---

## Frontend examples

<% frontend_examples.each do |example| %>
### <%= example['filename'] %>
```json
<%= example['json'] %>
```
<% end %>

<% end %>
